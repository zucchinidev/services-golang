// Package mid provides HTTP-specific middleware for APIs.
// These middleware components are protocol-specific and belong to the API layer.
// This is an acceptable package name since we're focusing on what the package does
// (provides middleware) rather than what it contains.
package mid
